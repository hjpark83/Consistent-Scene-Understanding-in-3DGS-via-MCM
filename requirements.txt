# The maintained GG environment uses Python 3.9 and
# torch==2.4.1/torchvision==0.19.1 from the CUDA 11.8 PyTorch index.
# Install PyTorch separately before this file.

# Core numerics and image processing
numpy==2.0.2
scipy==1.13.1
scikit-learn==1.6.1
opencv-python==4.12.0.88
pillow==10.4.0
tqdm==4.67.1
plyfile==1.1.3
matplotlib==3.9.4
PyYAML==6.0.2

# Segmentation, features, and depth backbones
transformers==4.57.1
timm==1.0.21
huggingface-hub==0.36.0
safetensors==0.6.2
pycocotools==2.0.10
groundingdino-py==0.4.0
open-clip-torch==3.3.0
git+https://github.com/facebookresearch/segment-anything.git

# Training and evaluation utilities
lpips==0.1.4
wandb==0.22.3

# Build helpers. Local CUDA extensions are installed separately after this
# file so TORCH_CUDA_ARCH_LIST can be set explicitly for the target GPU.
ninja==1.13.0
packaging==25.0
